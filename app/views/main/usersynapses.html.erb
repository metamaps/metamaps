<div class="everything" id="everything">
  <% @all.each do |object| %>
    <%= render object %>
  <% end %>
  <% if @all.empty? %>
	 <p><br>You haven't added any synapses yet.<p>
	<% end %>
</div>
<div class="clearfloat"></div>
