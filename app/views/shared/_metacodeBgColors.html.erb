<style>
<% Metacode.all.each do |m| %>
	<% if m.color %>
		<%= ".mbg" + m.name.gsub(/\s+/, "") + "{" %>
		<%= "background-color:" + m.color + " !important;" %>
		<%= "}" %>
	<% end %>
<% end %>
</style>